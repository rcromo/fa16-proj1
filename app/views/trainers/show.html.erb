<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<p>Pokemon: </p>
  <table class="table">

    <tbody>
      <tr>
        <th>Pokemon Name</th>
        <th>Pokemon Level</th>
        <th>Pokemon Health</th>
        <th> Damage </th>
      </tr>
      <% @pokemons.each do |pokemon| %>
        <tr>
          <td> <%= pokemon.name %> </td>
          <td> <%= pokemon.level %> </td>
          <td> <%= pokemon.health %> </td>
          <td> <%= button_to "Damage!", damage_path(id: pokemon),
           :class => "button medium", :method => :patch %>
          </td>
       </tr>
      <% end %>
    </tbody>
  </table>
</div>
